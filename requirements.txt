# Verified combination: streamlit 1.63.0, pandas 2.3.3, numpy 2.4.4, plotly 7.0.0
# (headless AppTest across the default view, All types, Choose-specific, and narrow
# year ranges — 0 exceptions on every path). Ranges below are deliberately open at the
# top except for pandas: pandas 3 changes replace/fillna behaviour the cleaning pipeline
# relies on, so that ceiling is load-bearing, not cosmetic.
streamlit>=1.49,<2
pandas>=2.1,<3
numpy>=1.26,<3
plotly>=5.22,<8
